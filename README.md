# BD-Final-PA

create Database distripizza;
Use distripizza
create table sesiones(
id INT auto_incrementprimary
key,
usuario VARCHAR(100),
contra VARCHAR(100),
);

Use distripizza;
show tables;
describe sesiones;
select * from sesiones;
